# OPIc 모의고사

> **실전과 같은 훈련으로 영포자의 승리자가 된다**

누적 다운로드: [![누적 다운로드](https://img.shields.io/github/downloads/SILVERBRINE/opic-mock-exam/total?label=downloads&cacheSeconds=60)](https://github.com/SILVERBRINE/opic-mock-exam/releases)

[⬇️ 최신 버전 다운로드](https://github.com/SILVERBRINE/opic-mock-exam/releases/latest)

🎧 문제는 귀로 듣고, 🎙️ 답변은 마이크로 녹음하면 됩니다.<br />
✅ 연습모드는 문항마다 피드백을 받고, API 모의고사는 끝까지 답변한 뒤 한 번에 결과를 확인합니다.<br />
🧰 OPIc 연습용 보조도구라서 서버 없이 HTML 하나로 실행됩니다.<br />
⚙️ 오프라인 평가와 API 평가 중 편한 방식을 선택하면 됩니다.<br />
📌 실제 OPIc 시험의 문항·난이도·진행·채점과는 다를 수 있으니 실전 대비 연습용으로 활용해 주세요.

## 다음 업데이트 준비 중

- API 모의고사: 전사 후 제출 시 5문항씩 채점, 성공한 묶음은 재시도에서 제외
- 최대 2문항 건너뛰기와 채점 제외, API 실패를 임시 점수로 평균내지 않음
- Gemini Auto를 3.5 / 3.1 Flash-Lite로 제한
- 브라우저 음성 인식이 종료되어도 원본 녹음 유지, 자동 권한 재요청 방지

## v1.4 업데이트

- 모의고사는 녹음 종료 후 다음 문항으로 바로 이동하며 전사·평가를 백그라운드에서 처리합니다.
- 문항 번호에 전사 중·완료·실패 상태를 색상으로 표시하고, 제출 시 남은 처리를 마무리합니다.
- Gemini 사고 수준 미지원 오류를 자동 복구합니다.
- 연습모드는 이어하기 없이 새로 시작하며 완료한 연습 기록은 유지합니다.

- Gemini 응답 스키마·재시도·Auto 대기 상한, OpenAI 모델별 요청 형식 보강
- Whisper 실제 실행에서 확인된 워커 로딩과 전사 출력 수집 오류 수정
- 녹음·전사 실패 시 원본 저장과 키 재연결, 미처리 녹음의 제출 누락 방지
- 저장소 차단·손상된 기록 복원·업데이트 중 새로고침·작은 화면 레이아웃 개선
- 규칙 평가의 임시 점수 표시, 규칙 평가가 포함된 예상 등급은 판정 보류
- 배포 전 회귀 테스트 자동 실행 구성

전체 검증 상태와 실행 범위는 [출시 점검표](docs/RELEASE-READINESS.md)에 기록합니다.

## v1.3.2 업데이트

- 문항별 제한 시간 3분, 녹음 종료 시 타이머 일시 정지
- 연습모드는 답변 전사와 평가 완료 후 종료 가능
- Gemini 빈 전사 응답을 한 번 재시도하고 실패 사유를 화면에 표시
- 실패한 녹음을 현재 탭에 보관해 재전사 가능하며, 전사 실패를 무응답 0점으로 처리하지 않음
- API 키와 연결 정보를 같은 탭의 새로고침·평가 방식 변경 후에도 유지
- 최종 문항 점수 표시 수정 및 릴리즈에 서비스 워커 포함

## v1.3.1 업데이트

- Gemini 3 계열 요청에서 `temperature`를 제거하고 모델별 `thinkingLevel`을 적용해 빈 응답 가능성을 낮춤
- Gemini 2.5 Flash는 불필요한 사고 토큰을 끄고, Gemini 2.5 Pro와 Gemini 3.1 Pro는 지원 범위에 맞는 설정을 사용
- Gemini 평가 응답에서 사고용 파트·코드블록·trailing comma를 안전하게 처리하고, JSON 파싱 실패 시 한 번 재시도
- 파싱 실패 원인을 차단·종료 사유와 함께 표시해 온라인 평가 실패 원인을 확인하기 쉽게 개선

## v1.3 기능

- 연습모드에서 문항 음성을 제한 없이 반복 재생
- Kokoro 로컬 TTS와 200문항 녹음본 캐시 지원
- 200문항 녹음본 일괄 생성은 PC 사양과 브라우저에 따라 30분 이상 걸릴 수 있음
- 일괄 생성 로딩창에 `0/200`부터 문항별 진행 상황 표시
- 생성한 WAV는 구조·신호·브라우저 디코딩을 확인하고, 문제가 있으면 자동 재생성
- Kokoro 추론과 WAV 변환을 Web Worker에서 실행해 생성 완료 때의 화면 멈춤을 줄임
- 사양이 충분한 PC에서는 Kokoro 워커를 최대 8개까지 문항 병렬 생성에 사용하고, 메모리 압박이나 워커 오류가 생기면 준비된 워커 수를 자동으로 줄임
- 기본 설정을 녹음본 + Gemini API로 제공하고, 홈 화면에서 GitHub 개발자 페이지로 이동 가능
- API·Whisper 녹음은 마이크 스트림을 세션 동안 재사용하며, 종료 후 전사·평가는 화면을 막지 않는 백그라운드 작업으로 처리

## 주요 기능

- OPIc 유형의 영어 질문 200개 문제은행과 역할극 문항
- 질문은 음성으로만 제공되며, 모의고사는 한 문항당 최대 2회, 연습모드는 제한 없이 반복 재생
- 브라우저 TTS, Kokoro 로컬 TTS, Kokoro가 만든 문항 녹음본(WAV 캐시) 중 선택
- 로컬 TTS를 한 번 준비하면 200문항 녹음본을 한 번에 미리 만들거나 필요한 문항만 자동 생성 가능 (PC 사양과 브라우저에 따라 전체 생성에 30분 이상 걸릴 수 있음)
- 녹음본은 저장 전후에 WAV 헤더·재생 시간·무음 여부·실제 디코딩을 검사하며, 실패한 문항은 최대 3회까지 다시 생성
- 음성 답변 원문은 수정할 수 없게 고정
- 문항 듣기를 누른 시점부터 문항별 3분 타이머 시작
- 마이크 스트림을 세션 중 재사용해 반복 권한 요청 최소화
- 모의고사는 녹음 종료 후 다음 문항으로 이동하고 전사·평가는 백그라운드에서 완료
- 진행 중인 문항만으로 즉시 제출하는 모의고사 모드
- 한 문항 피드백 후 새 문제를 계속 푸는 연습모드
- Intermediate Low부터 Advanced Low까지의 OPIc 예상 등급 표시

## 실행

1. 최신 릴리즈의 ZIP을 내려받아 압축을 풀어 주세요. `index.html`과 `coi-serviceworker.js`를 같은 폴더에 둡니다.
2. Chrome이나 Edge에서 실행해 주세요. 파일을 직접 열어 기본 기능을 사용할 수 있지만, 로컬 Whisper는 HTTPS 또는 `localhost` 환경이 필요합니다. 설치 없이 시작하려면 [웹에서 실행](https://silverbrine.github.io/opic-mock-exam/)할 수 있습니다.
3. 기본 추천은 **녹음본 + Gemini API** 조합입니다. 아래에서 평가 방식과 문항 음성 방식을 바꿀 수 있습니다.
   - **오프라인 기본 평가**: 인터넷 없이 규칙 기반 평가
   - **오프라인 Whisper**: `whisper.cpp`의 양자화 `base.en Q5_1` 모델(약 57MB)을 브라우저 저장소에 내려받아 로컬 전사
   - **OpenAI API / Gemini API**: 전사와 답변 평가가 더 정확하고 고품질로 나올 수 있지만 개인 API 키가 필요함

모의고사는 녹음 종료 후 바로 다음 문항으로 이동할 수 있습니다. API 모의고사는 전사만 백그라운드에서 진행하고 전체 제출 시 5문항씩 채점하며, 상단 문항 번호는 전사 중 노랑, 전사 완료 후 초록, 실패 시 빨강으로 표시됩니다. 전체 제출은 처리 중에도 가능하며 남은 작업을 마친 뒤 결과를 확정합니다. 전사 실패 녹음이 있으면 복구 화면을 표시합니다. 연습모드는 이어하기 없이 새로 시작하고, 문항별 피드백을 확인한 뒤 다음 연습으로 넘어갑니다. 완료한 연습 기록은 유지됩니다.

문항 음성 방식은 홈 화면의 **문항 음성 방식**에서 고르면 됨.

- **브라우저 TTS**: 별도 다운로드 없이 운영체제 음성으로 바로 재생
- **로컬 TTS**: Kokoro-82M 양자화 모델(약 86MB)을 브라우저에 내려받아 WebGPU 또는 WASM으로 PC 안에서 생성
- **녹음본**: Kokoro가 만든 질문 WAV를 IndexedDB에 캐시해 다음부터 파일처럼 재생. **200문항 녹음본 미리 만들기**를 누르면 한 번에 준비하고, 누르지 않으면 필요한 문항을 첫 재생 때 자동 생성합니다. 전체 생성은 PC 사양과 브라우저에 따라 30분 이상 걸릴 수 있습니다.

녹음본 일괄 생성은 논리 코어 수·장치 메모리·WebGPU 지원·JS 힙 압박·입력 지연·최근 생성 결과를 보고 동시 생성 수를 1~8개 범위에서 조절합니다. 시작은 안전하게 2개로 하고, 성공한 배치가 이어지면 준비된 워커 수까지 단계적으로 늘립니다. Kokoro 추론과 WAV 변환은 가능한 경우 Web Worker에서 처리하므로 메인 화면을 계속 사용할 수 있습니다. 메모리가 부족하거나 브라우저가 Worker를 지원하지 않으면 준비된 슬롯만 유지하거나 메인 스레드 호환 모드로 전환됩니다.

로컬 TTS 모델과 질문 녹음본은 사용자 답변 음성과 별개입니다. 질문 음성은 브라우저에만 저장되고, 사용자의 답변 원본 녹음은 전사 후 메모리에서 해제됩니다.

오프라인 Whisper 모델은 브라우저의 IndexedDB에 저장돼요. 첫 준비 때 whisper.cpp 실행 파일과 모델을 외부에서 내려받고, 이후에는 캐시된 모델을 재사용합니다. Kokoro 모델과 질문 녹음본도 브라우저 캐시에 저장됩니다. 비공개 모드에서는 브라우저를 닫을 때 저장 데이터가 사라져서 다시 받을 수 있습니다.

규칙 평가는 인터넷 없이 동작하지만, Whisper를 준비하지 않은 상태에서 사용하는 브라우저 음성 인식은 브라우저에 따라 인터넷이 필요할 수 있습니다. 완전한 오프라인 전사는 Whisper를 먼저 준비해 주세요. 오프라인 규칙·소형 AI 점수는 참고용이며 예상 등급을 확정하지 않습니다.

전사에 실패한 녹음은 현재 탭에서 재시도하거나 파일로 저장할 수 있습니다. 페이지를 새로고침하거나 닫으면 복구용 녹음은 사라지므로 먼저 저장해 주세요. 답변 텍스트와 연습 기록은 별도 JSON 백업으로 옮길 수 있습니다.

## Gemini API 키 발급 [무료]

API 방식을 쓰면 오프라인 기본 평가보다 음성 전사와 답변 분석이 더 정밀해져서, 연습 피드백 품질을 높일 수 있음. Google AI Studio의 무료 사용량과 정책은 계정·시점에 따라 달라질 수 있으니 발급 화면에서 확인하면 됨.

[Google AI Studio API 키 발급 페이지](https://aistudio.google.com/app/apikey)

1. Google 계정으로 로그인
2. **API key 만들기** 클릭
3. 키 복사해서 앱의 Gemini API 입력란에 붙여 넣기
4. **연결** 누르면 사용 가능한 최신 Flash 모델 확인 가능

Gemini **Auto · 3.5 / 3.1 Flash-Lite 교대**는 연결 목록에 있는 `gemini-3.5-flash-lite`와 `gemini-3.1-flash-lite`만 교대로 사용합니다. 한 후보가 일시적으로 실패하면 다른 후보를 시도하며, 둘 다 사용할 수 없으면 답변을 보존합니다. API 모의고사는 제출 시 5문항씩 채점하고, 실패한 경우 전체 제출로 미채점 묶음만 다시 요청할 수 있습니다. 같은 채점 기준을 적용하지만 모델 간 점수의 완전한 일치는 보장하지 않습니다. 탭 내부 요청 집계는 계정 전체 잔여량이 아닙니다.

API 키는 Google API로 직접 전송됩니다. 같은 탭의 세션 저장소에 제공자별로 보관하므로 새로고침과 평가 방식 변경 후에도 유지됩니다. 공용 PC에서는 사용 후 키 삭제를 눌러 주세요. 브라우저의 탭 복원 기능은 세션을 복원할 수 있습니다.

히스토리는 같은 Pages 주소의 브라우저 저장소에 자동으로 이어집니다. 새로 받은 HTML이나 다른 브라우저로 옮길 때는 앱의 **히스토리 → 기록 백업**으로 JSON을 내려받은 뒤, 새 환경에서 **백업 가져오기**를 누르면 기존 기록과 합쳐집니다.

로컬 Whisper WASM은 `coi-serviceworker.js`가 있는 HTTPS Pages 또는 `localhost`에서 준비하는 것이 안정적입니다. 파일을 직접 열거나 비공개 모드에서 실행하면 서비스 워커·IndexedDB가 제한될 수 있어 브라우저 음성인식과 규칙 평가로 자동 대체됩니다.

## 구조

```text
index.html                         정적 앱 진입점·UI·질문 데이터
docs/ARCHITECTURE.md                실행·평가·저장 흐름
```

질문 문제은행 200개는 별도 문서가 아니라 `index.html` 안에 들어 있어요. 앱의 **연습 질문 수정**에서 바꾼 내용은 지금 쓰는 브라우저에만 저장됩니다.

앱은 다음 순서로 동작합니다.

```text
브라우저 TTS 또는 Kokoro 로컬 TTS·캐시 녹음본으로 문항 재생
  -> 마이크 녹음
  -> 브라우저 MediaRecorder로 원본 확보
  -> 오프라인 Whisper 또는 선택한 API로 전사
  -> 규칙 평가 + (선택) 로컬 LLM / 온라인 AI 평가
  -> 문항 피드백 저장
```

API를 선택해도 별도 중계 서버를 거치지 않습니다. 따라서 사용자의 API 키와 음성 데이터는 선택한 제공자의 브라우저 API 요청으로 직접 전달됩니다.

## 라이선스

MIT License. 질문 콘텐츠와 외부 모델의 이용 조건은 각 원저작자와 제공자의 정책을 따릅니다.
